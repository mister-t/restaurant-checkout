const Spinner = () => {
  return (
    <div class="h-screen flex items-center justify-center">
      <div class="w-40 h-40 border-l-2 border-gray-900 rounded-full animate-spin"></div>
    </div> 
  )
};

export default Spinner;